apiVersion: v1
kind: Service
metadata:
  name: {{ include "spark.fullname" . }}-profile
  labels:
    {{- include "spark.labels" . | nindent 4 }}
spec:
  type: {{ .Values.profile.service.type }}
  ports:
    - port: {{ .Values.profile.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spark.selectorLabels" . | nindent 4 }}
    microservice: profile
