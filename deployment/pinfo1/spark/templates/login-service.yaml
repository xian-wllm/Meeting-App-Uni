apiVersion: v1
kind: Service
metadata:
  name: {{ include "spark.fullname" . }}-login
  labels:
    {{- include "spark.labels" . | nindent 4 }}
spec:
  type: {{ .Values.login.service.type }}
  ports:
    - port: {{ .Values.login.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spark.selectorLabels" . | nindent 4 }}
    microservice: login
