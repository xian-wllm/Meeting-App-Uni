version: "3.8"
services:
  messagerie:
    # image: registry.gitlab.unige.ch/p-info-2024/p-info-1/messagerie:latest
    build:
      context: .
      dockerfile: src/main/docker/Dockerfile.jvm
    environment:
      QUARKUS_DATASOURCE_JDBC_URL: jdbc:postgresql://spark:5432/spark
      QUARKUS_DATASOURCE_USERNAME: pinfo1
      QUARKUS_DATASOURCE_PASSWORD: pinfo1
    ports:
      - 8080:8080

  spark:
    image: postgres
    environment: 
      POSTGRES_USER: pinfo1
      POSTGRES_PASSWORD: pinfo1
      POSTGRES_DB: spark
    ports:
      - 5432:5432

      
networks:
  default:
    name: my-shared-network
    external: true
